function  log_cleaned = clean_log( t )
    global log
    log_cleaned = [];
    l1 =length(log(:,3));
    l =length(t);
    for i=1:l
        for j=1:l1
            if ((t(i) - log(j,3))<1e-5)
                log_cleaned = [log_cleaned; log(j,:)];
                flag =1;
            end
            if
        end
    end
    
end



